Question:
</br>
    Link: https://www.hackerrank.com/challenges/vector-sort/problem
    </br>
Solution:
</br>
    Here we learn the use of vectors and we declare them.We then use size(),push_back(),pop_back(), and sort() function to take input in the vector and then display it after sorting it in ascending order. 